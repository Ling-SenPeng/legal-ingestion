# Logging configuration for legal-injestion

# Suppress non-critical JBIG2 image warnings from PDFBox
org.apache.pdfbox.contentstream.PDFStreamEngine.level=WARNING

# Only log critical errors from PDFBox
org.apache.pdfbox.level=WARNING

# Root logger level
.level=INFO
